<h2>Login</h2>
<%= form_with model: @employee do |form| %>
  <%= form.label :email %>
  <%= form.text_field :email %>
  <%= form.label :password %>
  <%= form.password_field :password%>
  <%= form.submit "Login"%>
  <%= link_to "New employee? Signup", signup_path%>
<% end %>